{
  "name": "virtualbox-cli",
  "version": "1.0.0",
  "description": "Virtualbox CLI wrapper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "coffee -o ./build -c ./src/vbox.coffee"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "optimist": "^0.6.1"
  },
  "devDependencies": {
    "coffee-script": "^1.10.0"
  }
}
